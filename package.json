{
  "name": "express-j-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "build": "npm run clean && babel -d ./build src -s",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kipronofb@outlook.com",
  "license": "ISC",
  "dependencies": {
    "core-js": "^3.12.0",
    "dotenv": "^9.0.0",
    "express": "^4.18.2",
    "http-errors": "^1.7.2",
    "regenerator-runtime": "^0.13.7"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.16",
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.1"
  }
}
